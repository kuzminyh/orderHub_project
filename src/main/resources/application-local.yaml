spring:
  application:
    name: orderHub_project

  datasource:
    url: jdbc:postgresql://localhost:${POSTGRES_PORT:5435}/orderhub_prod
    username: orderhub_prod_user
    password: postgres
    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

#  flyway:
#    user: orderhub_user
#    password: postgres
#    enabled: false
#    locations: classpath:db/migration

  liquibase:
    user: orderhub_prod_user
    password: postgres
    enabled: true
    change-log: classpath:db/changelog/changelog_master.yaml


server:
  port: 8084

logging:
  level:
    org.hibernate.sql: DEBUG


